/**
 *
 */

angular.module( 'app.file.item', [])
    .controller( 'FileItemCtrl',
        function FileItemController(
            $scope, $filter, $stateParams,$rootScope,
            file, users, userSessionService,
            filesRestService, tasksRestService, commentsRestService
        ) {
           
        }
    )
    ;
    
