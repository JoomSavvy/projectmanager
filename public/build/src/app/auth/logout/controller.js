/**
 *
 */

angular.module( 'app.auth.logout', [])

    /**
     * And of course we define a controller for our route.
     */
    .controller( 'AuthLogoutCtrl', function AuthLogoutController( $scope,$filter, $stateParams) {

    })
    ;
    
