/**
 *
 */

angular.module( 'app.auth.login', [])

    /**
     * And of course we define a controller for our route.
     */
    .controller( 'AuthLoginCtrl', function AuthLoginController( $scope,$filter, $stateParams ) {
    })
    ;
    
